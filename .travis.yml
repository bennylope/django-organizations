language: python
python: 
  - 2.7
  - 3.4
  - 3.5
  - pypy
  - pypy3
env:
  - TOX_ENV=django10
  - TOX_ENV=django19
  - TOX_ENV=django18
  - TOX_ENV=flake8
install:
  - pip install tox
script:
  - tox -e $TOX_ENV
sudo: false
